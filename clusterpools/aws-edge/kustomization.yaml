---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: edge-pool

bases:
  # cloud provider and pull secrets
  - ../../kustomize/clusterpool

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: aws-install-config
    files:
    - install-config.yaml

resources:
  - namespace.yaml
  - clusterpool.yaml

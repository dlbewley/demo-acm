---
apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  annotations:
    policy.open-cluster-management.io/categories: Operators-Install
    policy.open-cluster-management.io/controls: ACS-Operator
    policy.open-cluster-management.io/standards: TofuOrg-Standards
  name: policy-acs-test
spec:
  disabled: false
  remediationAction: enforce # overrides remediationAction in policy-templates
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: policy-acs-test-ns
        spec:
          severity: high
          remediationAction: enforce
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Namespace
                metadata:
                  name: dale
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: policy-acs-test-secret
        spec:
          severity: low
          remediationAction: enforce
          object-templates:
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                data:
                  ca.pem: |
                    {{ fromSecret "stackrox" "collector-tls" "ca.pem" }}
                  collector-cert.pem: |
                    {{ fromSecret "stackrox" "collector-tls" "collector-cert.pem" }}
                  collector-key.pem: |
                    {{ fromSecret "stackrox" "collector-tls" "collector-key.pem" }}
                kind: Secret
                metadata:
                  name: collector-tls-test
                  namespace: dale
                type: Opaque